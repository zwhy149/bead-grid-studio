# Bead Grid Studio vX.Y.Z

Use this structure for every published Release. Replace every placeholder and remove instructions before publishing.

## What's new

- Describe user-visible changes in plain language.
- State explicitly when the conversion algorithm, project format, or palette values are unchanged.

## Who should update

Explain which users benefit from this release and whether an urgent update is needed.

## Download

For the offline app, download `bead-grid-studio-vX.Y.Z.html` from **Assets**. The ZIP is for redistribution with documentation. GitHub's automatically generated Source Code archives are not the offline app.

## Known limitations

- List limitations that materially affect the release.
- Link to [`docs/limitations.md`](limitations.md) for the complete maintained list.

## Checksums

Download `SHA256SUMS.txt` from this Release and compare it only with assets from the same Release.

```bash
sha256sum --check SHA256SUMS.txt
```
