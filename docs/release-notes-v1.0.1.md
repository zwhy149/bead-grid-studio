## License-complete portable release

- The online `NOTICE.txt` now exactly matches the repository `NOTICE`.
- The portable single-file HTML embeds the Apache-2.0 license and all third-party notices, so offline copies remain self-contained.
- CI now rejects notice drift and inconsistent application, health, PWA cache, or release version metadata.
- Automatic sizing copy now describes a recommended result without making an unsupported “clearest” claim.

No conversion algorithm, project format, or palette value changed in this patch.

## Integrity

Download `SHA256SUMS.txt` and verify the HTML or ZIP before redistribution.
