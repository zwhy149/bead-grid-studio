# Command-line (CLI) Example

Demonstrates invoking the `bead-grid` CLI tool programmatically or from shell scripts.

## Run

```bash
node examples/cli/demo.mjs
```

Or invoke the CLI directly:

```bash
node bin/bead-grid.mjs tests/fixtures/rocket-badge.png -w 29 -h 29 --format ascii
```
